[package]
name = "mona-tui-usage-overlay"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
chrono = "0.4"
crossterm = "0.29"
mona-usage-types = { path = "../mona-usage-types" }
ratatui = "0.30"
serde = { version = "1", features = ["derive"], optional = true }
serde_json = "1"

[features]
default = []
serde = ["dep:serde"]
