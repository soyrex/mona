[package]
name = "mona-provider-antigravity"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
mona-provider-gemini = { path = "../mona-provider-gemini" }
mona-schema-dialect = { path = "../mona-schema-dialect" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
