[package]
name = "mona-provider-copilot-runtime"
version = "0.1.0"
edition = "2024"
description = "GitHub Copilot provider runtime (direct API, tier detection, premium modes) for jcode, kept downstream of mona-base so provider edits do not rebuild the app spine"

[lib]
name = "mona_provider_copilot_runtime"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
# default-features = false: the top-level binary decides heavy optional base
# features (embeddings/bedrock). Runtime crates must not re-enable them via
# feature unification, or --no-default-features release targets (e.g. Windows
# ARM64, which cannot build tract-linalg asm) break.
mona-base = { path = "../mona-base", default-features = false }
mona-message-types = { path = "../mona-message-types" }
mona-provider-copilot = { path = "../mona-provider-copilot" }
mona-provider-core = { path = "../mona-provider-core" }
mona-provider-openai = { path = "../mona-provider-openai" }
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "charset", "http2", "system-proxy", "rustls-tls", "rustls-tls-native-roots"] }
serde_json = "1"
dirs = "5"
futures = "0.3"
tokio = { version = "1", features = ["sync", "time", "rt"] }
tokio-stream = "0.1"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
# The migrated copilot tests use mona-base's test-env sandbox.
mona-base = { path = "../mona-base", default-features = false, features = ["test-support"] }
tempfile = "3"
