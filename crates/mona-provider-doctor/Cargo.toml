[package]
name = "mona-provider-doctor"
version = "0.1.0"
edition = "2024"
publish = false

# Provider diagnostics that sit downstream of mona-base: the strict
# `jcode provider-doctor` end-to-end runner (provider_e2e), the live provider
# probes it drives, and the auth-lifecycle contract driver. Split out of
# mona-base so edits to the doctor cluster no longer rebuild the
# base -> app-core -> tui spine, and base compiles faster with lower peak
# memory.

[lib]
name = "mona_provider_doctor"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
futures = "0.3"
reqwest = { version = "0.12", default-features = false, features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["time"] }

# default-features = false: the doctor only needs base's always-on surface
# (auth, provider runtimes, provider_catalog, live_tests). Feature unification
# in the root binary re-enables embeddings/bedrock there.
mona-base = { path = "../mona-base", default-features = false }
mona-provider-gemini-runtime = { path = "../mona-provider-gemini-runtime" }
mona-provider-anthropic-runtime = { path = "../mona-provider-anthropic-runtime" }
mona-provider-openai-runtime = { path = "../mona-provider-openai-runtime" }
mona-provider-openrouter-runtime = { path = "../mona-provider-openrouter-runtime" }
mona-provider-cursor-runtime = { path = "../mona-provider-cursor-runtime" }
mona-provider-antigravity-runtime = { path = "../mona-provider-antigravity-runtime" }
mona-provider-copilot-runtime = { path = "../mona-provider-copilot-runtime" }

[dev-dependencies]
# The auth-lifecycle driver tests run inside mona-base's auth test sandbox.
mona-base = { path = "../mona-base", default-features = false, features = ["test-support"] }
mona-provider-metadata = { path = "../mona-provider-metadata" }
tokio = { version = "1", features = ["macros", "rt"] }
