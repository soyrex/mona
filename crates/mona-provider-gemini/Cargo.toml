[package]
name = "mona-provider-gemini"
version = "0.1.0"
edition = "2024"

[dependencies]
mona-message-types = { path = "../mona-message-types" }
mona-schema-dialect = { path = "../mona-schema-dialect" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
