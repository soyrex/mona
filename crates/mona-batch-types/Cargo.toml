[package]
name = "mona-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
mona-message-types = { path = "../mona-message-types" }
serde = { version = "1", features = ["derive"] }
